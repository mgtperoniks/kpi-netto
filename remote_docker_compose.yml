services:
  app:
    container_name: kpi-netto-app
    build: .
    restart: always
    ports:
      - "9003:80"
    env_file: .env
    networks:
      - kpinet

networks:
  kpinet:
    external: true
